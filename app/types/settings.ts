// Settings enums - must be in .ts file for runtime availability
export enum SettingValueType {
    STRING = 'STRING',
    NUMBER = 'NUMBER',
    BOOLEAN = 'BOOLEAN',
    JSON = 'JSON',
    DATE = 'DATE',
    ARRAY = 'ARRAY',
    UPLOADER = 'UPLOADER',
    SECTION = 'SECTION',
}

export enum AppDomain {
    DASHBOARD = 'DASHBOARD',
    ACADEMY = 'ACADEMY',
    SUPPORT = 'SUPPORT',
    SHARED = 'SHARED',
    SIGNATURE = 'SIGNATURE',
}
