/**
 * Re-export CompactCard from app/components so ui/compact-card/index resolves.
 * The component lives at ~/components/CompactCard.vue to avoid duplicate "Question" in auto-import names.
 */
export { default as CompactCard } from '~/components/CompactCard.vue';
